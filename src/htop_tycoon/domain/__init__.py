"""Domain layer: pure data types for the game state, engine, and persistence.

Phase 2A re-exports are added incrementally as each entity lands. This
package depends on nothing else inside htop_tycoon and is consumed by
the engine (Phase 2B) and UI (Phase 2C).
"""